# frozen_string_literal: true

RSpec.describe Try::Ruby::Parallel do
  it "has a version number" do
    expect(Try::Ruby::Parallel::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
